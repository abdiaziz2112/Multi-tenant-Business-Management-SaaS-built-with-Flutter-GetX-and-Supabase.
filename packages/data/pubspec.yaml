name: data
description: Repository implementations for the Ganacsi monorepo.
version: 0.1.0

publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core

  domain:
    path: ../domain