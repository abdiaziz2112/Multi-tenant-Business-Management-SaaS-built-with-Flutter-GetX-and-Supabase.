name: localization
description: en / so / ar translations and locale persistence (GetX Translations).
version: 0.1.0
environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"
dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  get_storage: ^2.1.1
